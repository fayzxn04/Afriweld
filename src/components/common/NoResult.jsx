/** @format */

import { Text } from "@mantine/core";

const NoResult = () => {
  return (
    <Text fz={24} fw={500} mt={20} ml={20}>
      No Data Found
    </Text>
  );
};

export default NoResult;
